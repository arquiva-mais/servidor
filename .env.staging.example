# ⚠️ STAGING ENVIRONMENT - NÃO USAR EM PRODUÇÃO
# Este arquivo é um template para o ambiente de staging
# Copiar para .env e ajustar os valores

# Node
NODE_ENV=staging
PORT=3003

# Database
DB_HOST=postgres-staging
DB_PORT=5432
DB_NAME=arquivamais_staging
DB_USER=postgres
DB_PASS=ArquivaMais2025Staging

# JWT Secrets (GERAR NOVOS para staging)
# Use: openssl rand -hex 32
JWT_SECRET=generate_a_new_64_char_secret_for_staging_environment_here
JWT_REFRESH_SECRET=generate_another_new_64_char_secret_for_refresh_tokens

# CORS
FRONTEND_URL=https://dev.arquivamaispenedo.online

# ⚠️ IMPORTANTE: Gerar novos secrets para staging
# NÃO usar os mesmos secrets de produção!
