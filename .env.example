# ⚠️  IMPORTANTE: Este é um arquivo de exemplo
# Copie para .env.production e configure com valores REAIS e SEGUROS

# Database Configuration
DB_NAME=arquivamais_db
DB_USER=arquiva
DB_PASS=USE_SENHA_FORTE_MIN_16_CHARS  # Ex: MyS3cur3P@ssw0rd2025!
DB_HOST=postgres  # 'postgres' no Docker, 'localhost' local
DB_PORT=5432

# Application
PORT=3001
NODE_ENV=production

# JWT Secrets - CRÍTICO: GERE NOVOS EM PRODUÇÃO!
# Gerar: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=SUBSTITUA_POR_SECRET_ALEATORIO_64_CHARS
JWT_REFRESH_SECRET=SUBSTITUA_POR_OUTRO_SECRET_ALEATORIO_64_CHARS

# Frontend URL - SEMPRE use HTTPS em produção
FRONTEND_URL=https://seu-dominio.com

# 🔒 LEMBRETE DE SEGURANÇA:
# 1. NUNCA commite o arquivo .env.production
# 2. Use senhas diferentes para cada ambiente
# 3. Altere a senha do admin após primeiro login
# 4. Rotacione secrets periodicamente (90 dias)
